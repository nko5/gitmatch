var test = require('tape');
var search_devs = require('../lib/search_devs');

test('Gettting repos for our possible developer', function (t) {
  t.plan(1);
  t.ok(false);
  // search_devs.getNRepos('h02e56', 'canvas-test2')
  // .then((repos) => {
  //   t.ok(repos, repos.length);
  // })
  // .catch((error) => {
  //   t.end(error);
  // });
});
